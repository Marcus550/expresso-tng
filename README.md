## Expresso TNG

### Segurança (GitHub)
- **NUNCA** suba `config.js` (ele contém as chaves do projeto).
- Copie `config.example.js` → `config.js` e preencha.

### Como rodar
Abra `index.html` no navegador (ou use um servidor estático).

# Expresso TNG - Site Institucional
Projeto de modernização digital para a empresa de logística Expresso TNG.